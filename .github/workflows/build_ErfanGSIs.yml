name: Building GSI..

on:
#  release:
#    types: [published]
#  push:
#    branches:
#      - master
#    paths:
#      - '.config'
#  schedule:
#    - cron: 0 8 * * 5
  watch:
    types: [started]
    
env:
  ROM_URL: https://dl.google.com/developers/android/rvc/images/factory/coral-rpb2.200611.009-factory-a236823f.zip
  ROM_NAME: Pixel
  ZIP_NAME: Pixel
  TZ: Asia/Kolkata
  MIR: bit
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
       - name: Checkout
         uses: actions/checkout@master

       - name: Initializing environment
         run: |
            sudo -E apt-get -qq update
            sudo -E apt-get -qq install git openjdk-8-jdk wget
            sudo apt-get install wget
       
       - name: Clone ErfanGSI Source Code
         run: git clone --recurse-submodules https://github.com/qrovic/ErfanGSIs -b master
       
       - name: Setting up ErfanGSI requirements
         run: |
              sudo chmod -R 777 ErfanGSIs
              cd ErfanGSIs
              sudo wget -O file.zip $ROM_URL
              sudo chmod 0777 dynamic.sh
             
       - name: Setting up ErfanGSI requirements
         run: |
              cd ErfanGSIs
              sudo bash setup.sh
              sudo bash ./dynamic.sh $ROM_NAME file.zip
              sudo ./make.sh system $ROM_NAME Aonly out

       - name: Print build info
         run: |
              cat ErfanGSIs/output/*-Aonly-*.txt
         
            
